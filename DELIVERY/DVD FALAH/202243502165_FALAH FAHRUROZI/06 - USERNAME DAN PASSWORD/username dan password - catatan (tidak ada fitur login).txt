FOLDER 06 — USERNAME DAN PASSWORD
==================================
HASIL AUDIT: APLIKASI TA TIDAK PUNYA FITUR LOGIN.

Aplikasi Dashboard DBSCAN (Streamlit) berjalan lokal di
http://localhost:8501 dan TIDAK memiliki autentikasi pengguna,
sehingga tidak ada username/password aplikasi yang perlu diisi.

Bagian ini boleh diisi (opsional — hanya jika dosen menanyakan
akun pendukung yang dipakai saat pengembangan):
- Akun Google (untuk Google Colab): ................
- Akun GitHub (untuk repo clustering-jobs): ........
- Lain-lain: .......................................

Jika tidak ada kredensial sama sekali yang dipakai, biarkan
catatan ini sebagai isi folder — itu sudah memenuhi folder 06.

(Template .docx "TEMPLATE Username dan Password - isi sendiri.docx"
ikut disertakan sesuai format pengumuman; abaikan bila tidak perlu.)
